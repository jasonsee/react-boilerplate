'use strict';

var React = require('react');

var App = React.createClass({

    render: function() {
        return (
            <h1>WAT React Boilerplate</h1>
        );
    }
});

module.exports = App;
